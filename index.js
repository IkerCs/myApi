const { getBot, searchBot } = require('./libs/bots');
const { MyBotListProfile, MyBotProfile } = require('./libs/profile');

module.exports = {
    getBot,
    searchBot,
    MyBotProfile,
    MyBotListProfile
}

